{
  "name": "6.todo-list",
  "version": "1.0.0",
  "description": "todo list project from The Odin Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --mode production",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  }
}
